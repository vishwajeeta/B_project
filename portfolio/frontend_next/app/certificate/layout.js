export const metadata = {
    title: "certificate -ASVSI",
    description: "vishwajeet Portfolio",
  };

  export default function RootLayout({ children }) {
    return (
      <html>
        <body>{children}</body>
      </html>
    );
  }